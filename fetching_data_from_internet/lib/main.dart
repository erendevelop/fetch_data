// Code gotta work but there is no such database as themoviedb anymore so it doesn't.
// But everything in the code is right to fetch some data from websites.
// Use it as you wish.
import 'package:flutter/material.dart';
import 'package:fetching_data_from_internet/homePage.dart';

void main() => runApp(MaterialApp(
      home: HomePage(),
    ));
